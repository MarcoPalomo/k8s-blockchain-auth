apiVersion: v1
kind: Secret
metadata:
  name: blockchain-auth-secret
  namespace: blockchain-auth
type: Opaque
stringData:
  # Adresse du smart contract déployé
  # À REMPLACER après le déploiement du contrat
  CONTRACT_ADDRESS: "REPLACE_WITH_YOUR_CONTRACT_ADDRESS"
